# Design-system notes

HTML concept mockups were removed on 2 Sep 2026. Live PHP templates plus `assets/css/shared.css` and `assets/js/shared.js` are the source of truth.

Brochure screenshots remain in `reference/` for historical layout comparison. Voice notes remain in `VOICE-GUIDE.md`.

## Spacing

When editing `assets/css/shared.css`, follow the SPACING SYSTEM comment in that file, then run:

```bash
python3 restwell-theme/tools/_check_spacing.py
```
