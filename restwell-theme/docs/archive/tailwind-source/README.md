# Archived Tailwind source (unused)

Live public CSS is `assets/css/shared.css`. These files are **not enqueued**.

- `input.css` — former Tailwind source
- `tailwind.css` — last compiled output
- `tailwind.config.js` — former config

Do not compile this back onto the front end. Do not add `restwell-tailwind` to `inc/enqueue.php`.
